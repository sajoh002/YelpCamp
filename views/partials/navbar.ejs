<nav class="navbar navbar-expand-lg navbar-dark sticky-top primary-color">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">HikeNationalParks</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/">Home</a>
        <a class="nav-link" href="/nationalParks">National Parks</a>
        <a class="nav-link" href="/nationalParks/new">New National Park</a>
        <% if (typeof nationalPark !== 'undefined') { %>
        <a
          class="nav-link"
          href="/nationalParks/<%= nationalPark._id %>/sights/new"
          >New Sight</a
        >
        <% } %>
      </div>
      <div class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
        <a class="nav-link" href="/login">Login</a>
        <a class="nav-link" href="/register">Sign Up</a>
        <% } else { %>
        <a class="nav-link" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
